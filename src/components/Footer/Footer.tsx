import { ReactElement } from "react";
import styles from "./Footer.module.scss";

const Footer = (): ReactElement => (
  <div className={styles.container}>
    <p>powered by NFT STUDIOS</p>
  </div>
);

export default Footer;
